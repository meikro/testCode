// 类型间的转换
// 字符串转为数值
Number("3");

// 布尔值转为字符串
String(false);

// 数组转为布尔值
Boolean([]);

// 数值转为对象
Object(3);

var n = 17;
binary_string = n.toString(2);
octal_string = "0" + n.toString(8);
hex_string = "0x" + n.toString(16);











